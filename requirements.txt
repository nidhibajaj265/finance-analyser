# Dashboard (Streamlit Cloud) dependencies only.
# The full pipeline dependency set (FinBERT, ChromaDB, etc.) lives in
# requirements-pipeline.txt and is installed by the GitHub Actions workflow.
streamlit==1.58.0
pandas==3.0.3
yfinance==1.4.1
newspaper3k==0.2.8
lxml_html_clean==0.4.5
supabase==2.31.0
huggingface_hub==1.18.0
tenacity==9.1.4
loguru==0.7.3
python-dotenv==1.2.2
pydantic==2.13.4
